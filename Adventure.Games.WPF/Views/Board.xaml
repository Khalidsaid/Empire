<UserControl
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ViewModels="clr-namespace:Adventure.Games.WPF.ViewModels" x:Name="ucBoard" x:Class="Adventure.Games.WPF.Views.Board" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" Loaded="Board_Loaded" >
    <Grid>
        <UniformGrid x:Name="uniformBoardGrid" Rows="{Binding RowCount, IsAsync=True, Mode=OneWay}" Columns="{Binding ColumnCount, IsAsync=True, Mode=OneWay}"/>
    </Grid>
    
</UserControl>
